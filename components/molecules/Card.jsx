import React from 'react'
import { styled } from 'styled-components'

const Card = ({ blog }) => {
    <Wrapper>

    </Wrapper>
}

const Wrapper = styled.article`
    width:320px;
    height:180px;
`

export default Card
